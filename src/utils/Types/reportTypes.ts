export interface WLTRProjectInitialParmas {
  pageNo: number;
  pageSize: number;
  sortColumn: string;
  GlobalSearch: string;
  isDesc: boolean;
  ProjectId: null | number | string;
  StartDate: string | null;
  EndDate: string | null;
}
